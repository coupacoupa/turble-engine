/**
 * Single entry point for the database schema.
 *
 * Auth tables are owned and managed by Neon Auth (in the `neon_auth`
 * Postgres schema) — they are not defined here. Application tables
 * (workflows, run events) will be added to this file.
 */
export {};
